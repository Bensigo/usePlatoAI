# Issue 400 Visual Evidence

Captured from the real Tauri desktop app, not the browser-only Vite surface.

- `wake-name-enabled-matched.png` - launched with `VITE_PLATO_WAKE_NAME_VISUAL_STATE=matched VITE_PLATO_INITIAL_VOICE_STATE=listening pnpm --filter @useplatoai/desktop tauri dev`; shows wake-name activation enabled for `Amber`, Vosk model-path state, and the voice loop in the listening activation result.

