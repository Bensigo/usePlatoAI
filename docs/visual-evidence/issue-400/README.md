# Issue 400 Visual Evidence

Captured from the foreground `usePlatoAI` Tauri desktop app with browser windows hidden, not from a browser-only Vite tab.

- `wake-name-enabled-matched.png` - launched with `VITE_PLATO_WAKE_NAME_VISUAL_STATE=matched VITE_PLATO_INITIAL_VOICE_STATE=listening pnpm --filter @useplatoai/desktop tauri dev`; shows wake-name activation enabled for `Amber`, detector matched state, the visible `Kill wake name` switch, and the voice loop in the listening activation result.
